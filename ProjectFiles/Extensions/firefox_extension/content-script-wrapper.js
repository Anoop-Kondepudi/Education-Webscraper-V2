// Define browser globally before loading the obfuscated script
if (typeof browser === "undefined") {
    var browser = chrome;
  }
  
  // Load the original script
  // The rest of the original 609265620450276.js content goes here